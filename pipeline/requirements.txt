# Install with a supported CPython (3.11--3.13):
# python -m pip install --index-url https://download.pytorch.org/whl/cpu -r pipeline/requirements.txt
# Use the CUDA selector on pytorch.org instead when training on an NVIDIA GPU.
torch==2.14.0
torchvision==0.29.0
